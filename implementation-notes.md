# 實作工作日誌（implementation-notes）

> 專案：商品效期管理 App（PWA 網頁版，安卓 + iPhone 共用同一份）
> 規格見 `規格文件.md`
> 最後更新：2026-06-03（v1.5 新增 iPhone 掃碼備援）

---

## [2026-06-03 v1.5 新增 iPhone 掃碼備援（ZXing）]

**目前版本：App v1.5 / SW Cache v9**

**背景：**
- 使用者詢問 iPhone 是否能掃條碼。確認現況：iPhone Safari 沒有原生 `BarcodeDetector`，原本程式一偵測到不支援就直接放棄、連相機都不開，退回手動輸入。
- 釐清重點：本 App 本來就是 PWA，安卓與 iPhone 共用同一份程式碼，不需要「做兩個版本再合併」。iPhone 的相機本身能開，缺的只是「自動讀碼」演算法。
- 使用者決定：先補 iPhone 掃碼（通知、語音在 iPhone 的系統限制暫不處理）。

**本次完成項目：**
- ✅ 下載 ZXing 函式庫本地檔 `js/zxing.min.js`（@zxing/library 0.21.3 UMD，約 336KB），確保離線可用、不依賴 CDN
- ✅ 改寫 `openScan()`：雙路徑
  - 路徑 A：有 `BarcodeDetector`（安卓/桌機 Chrome）→ 維持原邏輯，完全不變
  - 路徑 B：無 `BarcodeDetector`（iPhone Safari）→ 用 `ZXing.BrowserMultiFormatReader.decodeFromConstraints` 開後鏡頭掃碼
- ✅ `closeScan()` 新增 ZXing 引擎清理（`zxingReader.reset()` 釋放相機）
- ✅ `index.html` 加入 `<script src="js/zxing.min.js">`（在 app.js 之前載入）；版本文字 v1.4→v1.5
- ✅ `sw.js` 快取清單加入 `./js/zxing.min.js`；版本號 v8→v9
- ✅ `node --check` 語法檢查通過

**尚未完成 / 待使用者協助：**
- ⬜ 部署到 GitHub Pages（HTTPS 才能開 iPhone 相機）
- ⬜ iPhone 實機測試：Safari 開網址 → 點掃碼 → 相機開啟 → 自動掃到條碼
- ⬜ 安卓回歸測試：確認原生掃碼未被改壞

---

## [2026-05-30 v1.4 測試修正與功能完善]

**目前版本：App v1.4 / SW Cache v8**
**線上網址：https://eviltigercro.github.io/return/**
**操作指南：https://eviltigercro.github.io/return/guide.html**

**本次完成項目：**
- ✅ GitHub Pages 部署完成，網址正常運作
- ✅ 操作指南 HTML 頁面（guide.html）建立並上線
- ✅ SW 快取版本自動更新機制（v4→v8），每次改動都更新
- ✅ App 版本號顯示於設定頁（v1.4）
- ✅ 新版本提示橫幅（偵測到新版自動出現「立即更新」按鈕）
- ✅ 改善測試通知內容（顯示每日提醒時間）
- ✅ 行事曆提醒強化：
  - 新增商品後自動跳出「加入行事曆」對話框
  - 提醒中心新增「批量加入所有到期商品至行事曆」按鈕
  - 設定頁新增建議說明文字
  - 手機改用 Web Share API（分享選單），電腦退回下載
- ✅ 語音日期格式新增：「二零二六八月十五」（年、日字可省略）
- ✅ 刪除商品後詢問是否同步移除行事曆（下載取消 .ics）
- ✅ 操作指南全面更新，與 v1.4 功能同步

**下次接續項目：**
- 繼續測試各功能是否正常
- 如有新的測試回饋再逐一修正

---

## [2026-05-30 GitHub Pages 部署任務]

**執行內容：**
- 使用者詢問如何讓其他人用手機測試，說明了 localStorage 各自獨立的原理。
- 決定部署到 GitHub Pages（免費、固定網址、不需電腦開著）。
- 使用者 GitHub 帳號：`eviltigercro`
- 已完成：
  - ✅ 在 GitHub 建立新 Repository：`eviltigercro/return`（Public）
  - ✅ 設定 git remote：`https://github.com/eviltigercro/return.git`
  - ✅ `git push -u origin master` 成功，程式碼已上傳至 GitHub
- 進行中：
  - ⬜ 在 GitHub Settings > Pages 開啟 GitHub Pages（選 master branch）
  - ⬜ 確認部署網址：`https://eviltigercro.github.io/return/`

**部署完成後的測試網址：**
`https://eviltigercro.github.io/return/`

**下次接續項目：**
- 確認 GitHub Pages 是否成功開啟
- 用手機測試該網址是否正常運作
- 確認語音、相機（需 HTTPS，GitHub Pages 已是 HTTPS）正常

---

## [2026-05-30 讀取進度任務]

**執行內容：**
- 使用者再次輸入「讀取進度」。
- 我已使用工具完整重新讀取 `implementation-notes.md`（含 2026-05-29 日誌）與 `規格文件.md`。
- 執行 `git status` 確認目前工作區乾淨（nothing to commit, working tree clean），自 2026-05-28 優化建議審查報告以來，**完全沒有任何程式碼或檔案更動**。
- 同時使用 list_dir 確認根目錄結構與上次一致（無新增/刪除檔案）。
- 向使用者提供「濃縮版進度總覽」，讓您不用重新閱讀整份長日誌，即可掌握：
  - 目前功能完成度
  - 測試覆蓋狀況
  - 最大風險與落差
  - 2026-05-28 審查報告中仍待您決定的優先事項
- 嚴格遵守 Claude.md 三大規範：以繁體中文回應、邊做邊更新本日誌、絕不主動修改任何原始碼。

**已更新 implementation-notes.md 記錄本次行動。**

---

## [待釐清問題]

- 延續 2026-05-28 [優化建議審查報告] 的 Q1：使用者是否已閱讀完畢，並決定要優先處理哪 2~3 項？
  - A. 通知可靠性（最接近原規格的核心價值）
  - B. 大量資料（500~1000 筆）時的效能
  - C. 避免誤刪資料的安全機制
  - D. 程式碼架構長期可維護性
  - E. 其他（請直接告訴我）
- Q2. 若要繼續，您是否希望我「針對您選定的項目，先產出具體實作計劃 + 影響 vs 成本矩陣 + 程式碼改動範圍」供您審核？（我不會直接改 Code）
- Q3. 您現在是要：
  1. 繼續優化既有功能？
  2. 新增規格文件以外的新功能？
  3. 只是想快速確認現況，暫時不做事？

**若您不同意以上假設或想調整優先序，請直接告訴我，我會立刻修正。**

---

## [決定]

- 本次任務**純粹定位為「閱讀同步 + 接力準備」**，因此**完全不修改任何原始碼**（index.html / css / js / sw.js 等），也不主動提出新功能或重構建議。
- 假設使用者目的是「快速掌握現況後，決定下一步要不要動以及動哪裡」，我會在回應中提供「濃縮摘要 + 風險重點 + 待決清單 + 表格」，讓您用商業邏輯快速判斷。
- 所有待辦事項仍以 2026-05-28 審查報告中的「商業邏輯優先排序」為基礎（先保護「不會錯過退貨」「不會誤刪資料」）。

---

## [偏離]

- 無偏離。本次完全依照 Claude.md 規範進行閱讀、記錄與回應，無任何實作更動、也無擅自假設新需求。

---

## [取捨]

- 回應採「重點濃縮 + 表格化 + 問題清單」而非全文複製日誌，讓您能用商業邏輯在 1~2 分鐘內判斷。代價是極細節的歷史過程需自行查閱原始 `implementation-notes.md`（成本：低，可接受）。
- 選擇「先更新日誌再給回應」，確保「邊做邊更新」規範被嚴格遵守。
- 不主動幫您排序優先級（雖然我有商業邏輯建議），把決定權完全交給您。

---

## [2026-05-30 功能優化與新增建議任務]

**執行內容：**
- 使用者詢問：「以你的建議，可以在優化或者是多增加什麼功能」。
- 我已重新深入檢視目前實際程式碼（js/app.js 共 821 行、index.html、css/styles.css、sw.js），包含：
  - 兩層資料模型實作
  - 語音辨識與日期中文解析
  - 行事曆 .ics 產生邏輯
  - 通知與 Service Worker 機制
  - 列表渲染、搜尋篩選、卡片 UI
  - 備份/還原流程
- 同時對照 `規格文件.md` 與先前 2026-05-28 審查報告，整理出「優化現有」與「新增功能」兩大類建議。
- 每項建議皆標註：
  - 商業價值（對店家/使用者的實際幫助）
  - 開發與維護成本
  - 是否偏離原規格
  - 對未來轉 Flutter 原生的影響
- 嚴格遵守 Claude.md 規範：**不直接修改任何程式碼**，只提供分析與建議，並更新本日誌。

**已更新 implementation-notes.md 記錄本次完整建議分析。**

---

## [待釐清問題]

- 使用者這次明確要求「優化或新增功能建議」，是否代表您已經看完 2026-05-28 的審查報告，並希望我**擴充更具體的建議清單**？
- 在我提供的建議中，您最在意的前 3 項是什麼？（我可以針對這 3 項，立即產出「詳細實作計劃 + 程式碼改動範圍 + 風險評估」）
- 您對「超出規格的新功能」持什麼態度？
  - A. 只要商業價值高、成本合理，就可以考慮納入 v1
  - B. 先把規格內的功能做到最好，再談新增
  - C. 完全不考慮超出規格的功能

**請直接告訴我您的優先順序與態度，我會立刻調整建議深度。**

---

## [決定]

- 本次任務**明確定位為「擴充與深化建議」**，而非實作。
- 我選擇從「實際程式碼現況」出發，而非只憑空想像，因此特別檢視了 821 行單一檔案的維護風險、localStorage 限制、通知 API 的實際行為等。
- 建議排序原則依然遵循「商業邏輯優先」：先保護「不會錯過退貨」「不會誤刪重要資料」這兩個最高痛點。
- 所有建議都會清楚區分「優化現有」與「新增功能」，並標註規格偏離度，讓您能用商業角度判斷。

---

## [偏離]

- **輕微偏離**：原 2026-05-28 報告中我曾說「暫不主動提出新功能建議」，但本次使用者**主動詢問**「可以多增加什麼功能」，因此我才提供「超出規格但有價值」的選項。這是回應使用者明確問題，而非擅自擴大範圍。
- 其餘部分完全符合規範：未修改任何程式碼、未擅自開始實作、完整記錄思考過程。

---

## [取捨]

- **取**：提供「影響 vs 成本」矩陣 + 規格偏離標註，讓不懂程式的使用者也能快速用商業邏輯決定。這是本次最大的價值。
- **捨**：沒有針對每一項給出「完整程式碼實作計劃」（因為您還沒選定優先項目）。若一次給太多細節，會增加您的閱讀負擔，也違反「只有在您批准後才產出計劃」的原則。
- 代價：部分細節（如具體要改哪幾行 code）需等到您選定項目後，我才會再產出。

---

## 2026-05-30 優化與新增功能建議清單（詳細版）

### 一、目前程式碼真實狀況簡要診斷

- app.js 共 **821 行**，單一檔案，尚未拆分。
- 兩層資料模型（products + batches）實作乾淨且正確。
- 語音日期解析、行事曆 .ics、燈號計算都做得很好，細膩度甚至超越規格。
- 最大結構性風險：**localStorage + 單檔**，未來若要支援大量資料或複雜查詢，會成為瓶頸。
- 最大商業價值落差：**通知可靠性**（這點在先前報告已詳細說明）。

### 二、高 ROI 優化建議（強烈建議優先處理）

| 優先 | 項目 | 商業價值 | 開發成本 | 維護成本 | 偏離規格？ | 影響轉 Flutter？ | 建議 |
|------|------|----------|----------|----------|------------|------------------|------|
| 1 | 刪除安全機制（最近刪除清單 + 30天內可還原） | 極高。防止手滑誤刪重要商品，店家安心感大幅提升 | 低～中（約 80~120 行） | 低 | 否（規格允許「手動刪除」，但沒禁止安全機制） | 低（原生也需要類似機制） | 最高優先 |
| 2 | 備份檔加入版本號 + 結構校驗（SHA-256 或簡單 checksum） | 高。避免未來欄位異動時，還原壞掉資料 | 低（約 30~50 行） | 低 | 否 | 低 | 非常值得 |
| 3 | 通知到達時「強制開啟提醒中心」+ 更清楚文案 | 高。讓使用者真正感受到「退貨提醒」的價值 | 低～中 | 低 | 輕微偏離（規格只說「發通知」） | 低 | 值得 |
| 4 | 增加「標記為已處理」（隱藏而非刪除，保留歷史） | 高。可減少誤刪，也保留進貨記錄供日後查詢 | 中 | 中 | 是（規格只提刪除） | 中 | 建議列入 |
| 5 | 每日開 App 時，若有到期商品，自動跳出「今日到期總覽」 | 中～高。降低「忘記看提醒中心」的風險 | 低 | 低 | 輕微偏離 | 低 | 簡單好用 |

### 三、中 ROI 優化建議

| 優先 | 項目 | 商業價值 | 開發成本 | 維護成本 | 偏離規格？ | 影響轉 Flutter？ | 建議 |
|------|------|----------|----------|----------|------------|------------------|------|
| 6 | 列表虛擬化或簡單分頁（超過 300 筆自動分頁） | 中。預防 800~1000 筆時列表卡頓 | 中～高 | 中 | 否 | 高（原生做法不同） | 只有資料量真的會很大才做 |
| 7 | 掃碼畫面增加「手電筒開關」按鈕 | 中。手機在倉庫/貨架暗處掃碼時非常實用 | 低～中 | 低 | 否 | 低 | 值得做 |
| 8 | 表單即時驗證 + 更友善的錯誤提示 | 中。減少使用者填錯後才發現 | 低 | 低 | 否 | 低 | 基礎體驗優化 |
| 9 | 支援「批量選擇 + 批量標記已處理 / 批量刪除」 | 中～高。店家一次處理多筆時效率提升 | 中～高 | 中 | 是 | 中 | 視實際使用頻率 |
| 10 | 搜尋加強（支援條碼前綴、模糊比對、最近搜尋記錄） | 低～中 | 低 | 低 | 否 | 低 | 可做可不做 |

### 四、低 ROI / 高成本（不建議短期做）

- 完整重構拆分 app.js 成多個模組（目前 821 行還可接受，拆分後反而增加初學者閱讀門檻）
- 將 localStorage 全面改成 IndexedDB + 建立搜尋索引（除非真的有 2000+ 筆需求）
- 背景通知改用 Firebase / 自架推播服務（嚴重違反「單機版、不需網路」原則，成本極高）

### 五、值得考慮的新增功能（超出規格，但商業價值高）

這些項目**原本不在 v1 範圍**，但從店家實際使用角度，可能帶來明顯幫助：

| 項目 | 商業價值 | 開發成本 | 維護成本 | 對未來影響 | 我的看法 |
|------|----------|----------|----------|------------|----------|
| 匯出 Excel / CSV（含燈號、退貨日等欄位） | 極高。很多店家習慣用 Excel 做報表或給會計 | 中 | 低 | 低 | 強烈建議列入 v1.5 或 v2 |
| 商品拍照上傳（記錄外觀、貨架位置、進貨照片） | 高。尤其是生鮮、食品，視覺記錄很有價值 | 高（需處理圖片壓縮、儲存） | 高 | 高（原生相機 API 差異大） | 建議留到原生版再做 |
| 統計報表頁（本月即將到期數量、已處理數量、平均提前天數） | 中～高。幫助店家了解自己進貨節奏 | 中 | 中 | 低 | 可以考慮簡單版本 |
| 批次合併功能（同商品不同批次想合併數量） | 低～中（使用情境較少） | 中 | 中 | 低 | 暫不建議 |
| 多分類 / 標籤（例如：冷藏區、常溫區、促銷品） | 中。大型店家會需要 | 中～高 | 中 | 中 | 建議等資料量大時再評估 |
| 支援「已退貨」狀態（不刪除，但標記為完成） | 高（與「標記已處理」類似，但更明確） | 低～中 | 低 | 低 | 可與「標記已處理」合併考慮 |

### 六、我的最終建議排序（供您參考）

**第一梯隊（最值得現在就做）**：
1. 刪除安全機制（Undo）
2. 備份檔結構校驗
3. 通知到達時強制開提醒中心
4. 「標記為已處理」功能

**第二梯隊（資料量成長後再評估）**：
- 列表分頁 / 虛擬化
- 掃碼手電筒

**第三梯隊（超出規格，建議留到 v2 或原生版）**：
- Excel 匯出（強烈建議，但可晚一點）
- 商品拍照
- 統計報表

---

**下一步請您直接告訴我**：

1. 上面表格中，您最想先深入了解的前 3 項是哪幾個？
2. 針對這 3 項，我是否要立刻產出「詳細實作計劃 + 預估改動行數 + 風險清單 + 對未來轉原生的影響」？
3. 您對「新增超出規格功能」的態度是 A / B / C？（見上方待釐清問題）

我會等您的明確指示，才會進一步產出任何實作相關文件或開始寫 Code。

（本段已同步記錄至 implementation-notes.md）

---

## [2026-05-29 讀取進度任務]

**執行內容：**
- 使用者輸入「讀取進度」。
- 我已完整重新讀取 `implementation-notes.md`（包含 2026-05-28 所有日誌與優化建議審查報告）以及 `規格文件.md`。
- 額外檢視 `package.json` 與 `js/app.js` 前 50 行，確認目前程式碼狀態與日誌描述相符（PWA 架構、兩層資料模型、localStorage 儲存等均一致）。
- 同時瀏覽專案根目錄結構，確認截圖、測試腳本、Service Worker 等輔助檔案仍在。
- 向使用者回報目前完整進度總覽，並明確指出上次審查報告中留下的待決優先事項。
- 嚴格遵守 Claude.md 規範：以繁體中文回應，並邊做邊更新本日誌。

**已更新 implementation-notes.md 記錄本次行動。**

---

## [待釐清問題]

- 使用者這次「讀取進度」後，是否準備好從 2026-05-28 [優化建議審查報告] 的 [待釐清問題] 中，選出 2~3 項最想優先處理的項目？
- 若要繼續優化，是否需要我先針對您選定的項目，產出「具體實作計劃 + 影響 vs 成本評估 + 程式碼改動範圍」？

## [決定]

- 本次任務定位為「純閱讀與進度同步 / 接力準備」，因此**不主動修改任何原始碼**，也不主動提出新功能建議。
- 假設使用者目的是快速掌握現況後決定下一步，我會在回應中提供「濃縮版進度摘要 + 風險重點 + 待決事項清單」，讓您不用重新閱讀整份長日誌即可判斷。

## [偏離]

- 無偏離。本次完全依照專案規範進行閱讀與記錄，無任何實作更動。

## [取捨]

- 回應中採「重點濃縮 + 表格化呈現」而非全文複製日誌，讓您能用商業邏輯快速判斷。代價是極細節的歷史過程需自行查閱原始檔案（成本：低，可接受）。
- 選擇先更新日誌再給回應，確保「邊做邊更新」規範被遵守。

---

## [2026-05-28 讀取檔案資料任務]

**執行內容：**
- 使用者要求「讀取這個檔案的資料」。
- 由於未明確指定檔名，我判斷最可能是專案核心文件 `規格文件.md`（需求規格的完整資料來源）。
- 已完整讀取該檔案，並提取結構化資料（產品概述、名詞定義、功能需求 FR-1~FR-7、資料結構、提醒邏輯、畫面清單、非功能需求等）。
- 同時檢視 `implementation-notes.md` 與 `js/app.js` 確認目前實作狀態與規格的一致性。
- 回應將以繁體中文呈現規格中的關鍵「資料」給需求方檢視。

**已更新 implementation-notes.md 記錄本次行動。**

---

## [目前進度（2026-05-27 暫停點 #3）]

**已完成的檔案：**
- `規格文件.md`、`implementation-notes.md`
- `index.html`（全部畫面結構）
- `css/styles.css`（全部樣式）
- `js/app.js`（核心邏輯：資料層、列表、新增/編輯/詳情/刪除、掃碼、語音、提醒中心與通知、設定、備份/還原）
- `manifest.webmanifest`（PWA 安裝設定）
- `sw.js`（Service Worker：離線快取 + 通知處理）

**2026-05-26 自動化測試結果（Playwright）：**
- ✅ App 載入正常，主列表為預設畫面
- ✅ 新增商品（7 欄位全部正常）
- ✅ 退貨日自動計算（有效日 − 提前天數）
- ✅ 燈號 🔴🟡🟢 正確顯示
- ✅ 排序（退貨日近→遠）、搜尋、篩選
- ✅ 詳情頁（全欄位含狀態文字）
- ✅ 編輯功能（帶入原有資料，儲存 toast「已更新」）
- ✅ 刪除功能（原生 confirm + 列表減少）
- ✅ 設定頁（預設天數 / 提醒時間 / 通知開關）
- ✅ 提醒中心（到期商品清單）
- ✅ 無任何 JS 錯誤

**已找到並修正的 Bug：**
- 🐛 `#scan-overlay` CSS `.overlay { display: flex }` 覆蓋 `hidden` 屬性，導致不可見的掃碼蓋層阻擋所有點擊
  → 修正：`css/styles.css` 加入 `[hidden] { display: none !important; }`

**2026-05-27 手機實機測試（ngrok HTTPS）：**
- ✅ 語音輸入名稱（A 手機正常）
- ✅ 語音輸入日期（A 手機正常）
- ✅ B 手機語音辨識失敗 → 已改善錯誤提示訊息（顯示具體原因）→ 已處理
- ✅ 相機掃條碼（正常）
- ✅ 備份匯出（正常）
- ✅ 備份還原（正常）
- ⬜ PWA 安裝成 App（暫緩）
- ⬜ 背景推播通知（暫緩）

**已找到並修正的 Bug：**
- 🐛 `#scan-overlay` 蓋層阻擋點擊 → 已修
- 🐛 手動輸入條碼無引導 → 已修（`focusBarcodeField()` + 閃光動畫）
- 🐛 語音錯誤訊息不清楚 → 已修（`voiceErrorMsg()` 依錯誤碼給中文說明）
- 🐛 Service Worker 快取導致手機看不到最新版 → 已修（版本號升至 v3）

**2026-05-27 新增功能：**
- ✅ 詳情頁「📅 加入行事曆（退貨日提醒）」按鈕，產生 .ics 檔
- ✅ 表單底部勾選框「📅 儲存後自動加入行事曆」
  - 新增商品時預設勾選（自動加）
  - 編輯商品時預設不勾（避免重複）
  - 兩個入口共用 downloadCalendarICS() 函式
- .ics 相容 Google / 三星 / 小米 / Apple / Outlook 行事曆

**2026-05-27 實機測試發現的 Bug（第三批修正）：**
- 🐛 手機 HTML/JS 版本不同步：openForm() 存取 #f-calendar 時 null → TypeError
  → 導致「+無法新增」、「編輯無反應」、「看不到勾選框」
  → 修正：openForm() / saveForm() 加 null 防護（optional chaining / if 判斷）
- 🐛 Chrome 下載同名 .ics 檔時跳「再次下載確認」
  → 修正：檔名加時間戳（精確到分鐘），每次下載都是唯一檔名
- SW 快取版本升至 v4，強制手機重新整理所有檔案

**手機清快取方式（版本不同步時使用）：**
- Chrome 右上角 ⋮ → 設定 → 隱私權與安全性 → 清除瀏覽資料 → 勾快取 → 清除

**下次接續項目：**
- 確認手機清快取後 +、編輯、勾選框都正常
- 如有其他功能需求再繼續
- 手機測試 ngrok：執行 `python -m http.server 8000`，再執行 `ngrok http 8000`

**注意事項：**
- 相機掃碼與語音需在 `localhost` 或 HTTPS 下才能用。
- 本機啟動：`python -m http.server 8000`，瀏覽器開 `http://localhost:8000/`
- 手機測試：加開 `ngrok http 8000`，用 ngrok 給的 https:// 網址

---

## [待釐清問題]

### Q1.（已解決）開發環境尚未安裝，要選哪條路徑起步？
- 現況：本機只有 git，**Flutter / Dart / Java(JDK) / Android Studio 皆未安裝**。
- ✅ 需求方決定：採 **路徑 B（PWA 網頁版）** 作為 v1 起步。

### Q2.（待釐清）手機端要怎麼實際使用？
- v1 先在桌機 `localhost` 測試（可用桌機網路攝影機試掃碼）。
- 手機要完整使用（相機/語音/安裝成 App）需在 **HTTPS** 環境，屆時需部署到靜態主機（如 GitHub Pages / Netlify）。
- 目前假設：先在桌機驗證功能，手機部署留到功能穩定後再處理。

---

## [決定]
- **採路徑 B：PWA 網頁版**作為 v1。
- 技術選型：純 HTML + CSS + JavaScript，**無建置工具**（不需 Node 編譯），可直接以瀏覽器執行。
- 資料儲存：瀏覽器 `localStorage`（JSON），符合單機、離線、1000 筆以內的需求。
- 掃碼：優先使用瀏覽器原生 `BarcodeDetector`（支援 EAN-13 等），不支援時自動退回手動輸入。
- 語音：使用瀏覽器 Web Speech API（Chrome/Edge）。
- 通知：Notification API + Service Worker；App 開啟時可靠檢查並提醒，背景提醒為「盡力而為」。
- 沿用先前決定（規格文件第十二節）：批次兩層資料結構、退貨日當天通知、手動刪除、備份匯出檔案。

---

## [偏離]
- **原訂 Flutter 原生 → 改為 PWA 網頁版。**
  - 理由：需求方選擇免安裝、可立即試用的方案。
  - 影響需求 4（自動提醒）：PWA 在「App 完全關閉時」的排程通知不可靠，故降級為「開啟 App 時一定能看到並提醒哪些到退貨日 + 盡力背景通知」。
  - 未來若要與原 Flutter 規格相同的可靠背景提醒，需轉原生重寫（成本：中～高）。

---

## [取捨]
- PWA 起步最快、零安裝、跨 Android/桌機；代價是背景自動通知不可靠。
- 相機與語音需在 `localhost` 或 HTTPS 安全環境才能用；`file://` 直接開會被擋 → 需跑本機伺服器測試。
- 用 localStorage 而非 IndexedDB：開發最快、足夠 v1；若未來資料量大或要複雜查詢，效能與彈性會受限。
- 不引入第三方掃碼函式庫（用瀏覽器原生 API）：維持離線、零相依；代價是舊瀏覽器可能不支援掃碼，需退回手動輸入。

---

## [2026-05-28 優化建議審查報告]

**執行背景**：使用者詢問「有建議優化的東西嗎？」。我已完整讀取規格文件 + 目前所有原始碼 + 代表性截圖 + 測試腳本，進行系統性審查。

### 目前實作總體評價
- **功能完整度**：對照規格文件，**核心功能已實現 90% 以上**（掃碼、語音、日曆、備份、兩層資料模型、燈號、搜尋篩選分組、設定全部到位）。
- **程式碼品質**：乾淨（無 TODO/console.log）、錯誤處理中文化、日期/語音解析細膩、UI 現代簡潔（從截圖看非常好）。
- **最大現有風險**：通知可靠性（已在先前 [偏離] 記錄）。這是「商品到退貨日自動提醒」這個核心商業價值的最大落差。

### [待釐清問題]
- Q1. 使用者現在最想優化「哪一類」？
  - A. 通知可靠性（最接近原規格的核心價值）
  - B. 大量資料（500~1000 筆）時的效能
  - C. 避免誤刪資料的安全機制
  - D. 程式碼架構長期可維護性
  - E. 其他（請告訴我）
- Q2. 是否願意接受「增加少量複雜度」來換取更好體驗？（例如加入 IndexedDB、虛擬列表、或外部提醒服務）
- Q3. 未來是否有機會轉回原生（Flutter）？若有，現在的優化方向要不要「盡量靠近原生架構」？

**若您不同意以上假設**，請直接告訴我優先序，我會立刻調整建議清單。修改成本：低（只是重新排序文件）。

### [決定]
- 採「**商業邏輯優先排序**」而非技術優先：先保護「不會錯過退貨」「不會誤刪重要資料」這兩個最高痛點，再談效能與架構。
- 提供「**影響 vs 成本**」矩陣，讓您（不懂程式的人）能用商業角度快速判斷。
- 暫不直接修改任何程式碼。除非您明確批准某項優化，我才會進入實作階段。
- 所有建議都會標註「是否偏離原規格」「是否影響未來轉原生」。

### [偏離]
- 本次審查再次確認：**通知機制仍維持「開 App 時可靠 + 背景盡力」**，與原規格「退貨日當天自動發本地通知」有落差。這不是新偏離，而是延續先前決定。
- 目前實作在「細節體驗」上其實**超越**了規格（例如自動加入行事曆、退貨日即時預覽、語音中文數字解析），這是正向偏離。

### [取捨]
**高 ROI（建議優先做）**
- 刪除安全機制（Undo / 最近刪除清單）：成本低、保護資料安全、使用者安心感高。
- 通知到達時的「醒目提醒中心」自動開啟 + 更清楚的文案。
- 備份檔加入版本號與資料校驗，避免未來結構變更時還原壞掉。

**中 ROI**
- 列表虛擬化或分頁（1000 筆時卡頓預防）。
- 掃碼增加手電筒控制。
- 增加「標記為已處理」（隱藏而不是刪除），減少誤刪。

**低 ROI / 高成本**
- 完整重構拆分 app.js 成多模組（現在 820 行還可接受）。
- 改用 IndexedDB + 搜尋索引（除非真的有 2000+ 筆需求）。
- 背景通知改用推播服務（需後端，違反單機原則）。

**已做得很好的地方（不需要優化）**
- 兩層資料模型實作正確且清晰。
- 語音日期解析對中文很友善。
- UI 卡片燈號、表單預覽、行事曆 .ics 都很實用。
- Service Worker 快取策略正確。

---

**下一步行動建議**：
1. 請您閱讀上面 [待釐清問題]，告訴我最想先處理哪 2~3 項。
2. 我會針對您選的項目，產出「具體實作計劃 + 影響評估 + 程式碼改動範圍」。
3. 只有在您明確說「可以開始改」後，才會動任何原始碼。

（本段已同步記錄本次完整分析過程）
